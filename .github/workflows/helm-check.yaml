name: Helm Chart Check

on:
  push:
    branches:
      - master
    paths:
      - "chart/falco/**"
      - ".github/workflows/helm-check.yaml"
      - ".github/kind-config.yaml"
      - "ct.yaml"
      - "Makefile"
  pull_request:
    branches:
      - master
    paths:
      - "chart/falco/**"
      - ".github/workflows/helm-check.yaml"
      - ".github/kind-config.yaml"
      - "ct.yaml"
      - "Makefile"

jobs:
  helm-check:
    name: Verify Helm Chart
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Set up Helm
        uses: azure/setup-helm@dda3372f752e03dde6b3237bc9431cdc2f7a02a2 # v5.0.0
        with:
          version: "4.1.1"

      - name: Setup Go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version-file: "chart/falco/tests/go.mod"
          check-latest: true

      - name: Install helm-docs
        run: go install github.com/norwoodj/helm-docs/cmd/helm-docs@v1.11.0

      - name: Verify chart README and unit tests
        run: HELM_DOCS="$(go env GOPATH)/bin/helm-docs" make chart-docs-check chart-unit-test

      - name: Setup Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.x"

      - name: Set up chart-testing
        uses: helm/chart-testing-action@6ec842c01de15ebb84c8627d2744a0c2f2755c9f # v2.8.0

      - name: Run chart-testing lint
        run: ct lint --config ct.yaml --charts chart/falco

      - name: Create KIND Cluster
        uses: helm/kind-action@ef37e7f390d99f746eb8b610417061a60e82a6cc # v1.14.0
        with:
          config: .github/kind-config.yaml

      - name: Run chart-testing install
        run: ct install --exclude-deprecated --config ct.yaml --charts chart/falco
