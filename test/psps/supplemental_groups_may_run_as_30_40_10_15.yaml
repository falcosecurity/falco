apiVersion: policy/v1beta1
kind: PodSecurityPolicy
metadata:
  name: supplemental_groups_may_run_as_30
spec:
  supplementalGroups:
    rule: "MayRunAs"
    ranges:
      - min: 30
        max: 40
      - min: 10
        max: 15
